{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "write-session",
  "owner": {
    "name": "androsland"
  },
  "metadata": {
    "description": "Bounded SESSION.md checkpoint for surviving /clear without paying to re-read the conversation."
  },
  "plugins": [
    {
      "name": "write-session",
      "source": "./",
      "description": "A Stop hook keeps git state and a capped tail of recent turns current at zero model tokens; the /write-session command adds the narrative half in a single request. Then /clear and resume with \"read SESSION.md and continue\"."
    }
  ]
}
